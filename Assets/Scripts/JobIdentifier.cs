using UnityEngine;

public class JobIdentifier : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is createdusing UnityEngine;
    public int jobNumber;
}
